var toDoItems = [];
var currentDate = moment().format("MMM Do");
var currentHour = moment().format("h");
var time = $(".time");

function background(){
    $(".form-control").each(function (){
        
    })
}